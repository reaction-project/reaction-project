section.container.grid-960
  .columns.justify-center
    .column.col-8.col-xs-12.col-sm-10
      .columns
        .column
          - if @actions.any?
            table.table
              tbody
                = render @actions
          - else
            h3.empty
              | There are no Action Items. #{link_to 'Add one?', new_admin_action_path}
      .columns
        .column
          = link_to 'New Action', new_admin_action_path, class: 'btn btn-primary'
